<?xml version="1.0" encoding="utf-8"?>
<!--
  ScrollCount manifest.

  Deliberately tiny permission list: the app never needs the internet, so the
  INTERNET permission is explicitly removed even if a library tries to add it.
  That is what makes the "nothing ever leaves your phone" promise provable.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Needed on Android 13+ to show limit alerts and the daily summary. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Hard guarantee: no library may pull in network access. -->
    <uses-permission android:name="android.permission.INTERNET" tools:node="remove" />

    <!--
      WorkManager declares this so that apps can run expedited jobs as a
      foreground service. ScrollCount never does, and carrying the permission
      would mean answering foreground-service questions at Play Store review
      for a feature that does not exist. So it is removed.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />

    <application
        android:name=".ScrollCountApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScrollCount"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ScrollCount">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Watches for new reels/shorts. Counts only; never reads content. -->
        <service
            android:name=".service.ScrollCountService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Lets other apps read the shareable stats card from our cache folder. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
