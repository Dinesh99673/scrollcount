<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the real UI; this theme only styles the window before it loads. -->
    <style name="Theme.ScrollCount" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
